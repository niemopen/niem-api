
spring:
  datasource:
    url: ${POSTGRES_URL_LOCAL}
    username: ${POSTGRES_USER_LOCAL}
    password: ${POSTGRES_PASSWORD_LOCAL}

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        default_schema: test
      jakarta:
        persistence:
          schema-generation:
            # create-source: metadata
            scripts:
              # action: create
              create-target: schema-test.sql
